<div align="center">
<h1>360d | 智能數據儀表板</h1>
<p>
    <b>Intelligent Web Scraping & Automation Dashboard</b><br>
    繁體中文 • 智能提取 • 自動排程 • 數據分析
</p>
</div>

---

## 📖 專案簡介 (Overview)
**360d** 是一個現代化的網頁數據提取儀表板，專為自動化監控與競品分析設計。
透過整合 **Gemini AI** 的強大理解能力與 **Python** 的自動化生態，360d 讓使用者能夠輕鬆將任何網頁轉換為結構化數據，並設定定期排程自動發送 Email 報告。

## ✨ 核心功能 (Features)
- **🎯 智能提取**: 支援「新聞文章」、「產品價格」、「公司資訊」等多種場景的 AI 自動識別。
- **⏱️ 自動排程**: 內建排程系統 (APScheduler)，可設定「每 X 天」自動執行任務。
- **📧 郵件通知**: 任務完成後自動寄送 HTML 格式的數據報告。
- **💾 歷史紀錄**: 完整保存所有執行結果 (SQLite)，隨時回顧。
- **📱 響應式設計**: 完美支援手機與桌面瀏覽。

## 🚀 快速開始 (Quick Start)

### 1. 安裝環境
確保已安裝 Python 3.9+。
```bash
pip install -r requirements.txt
```

### 2. 設定參數
建立 `.env` 檔案 (或在 Zeabur 設定環境變數)：
```ini
# AI 核心 (必填)
GEMINI_API_KEY=your_gemini_api_key

# 郵件通知 (選填，若要使用排程功能則必填)
SMTP_EMAIL=your_email@gmail.com
SMTP_PASSWORD=your_app_password
```
> **注意**: 若使用 Gmail，請務必至 Google 帳號設定開啟「兩步驟驗證」並產生「應用程式密碼 (App Password)」。

### 3. 啟動系統
```bash
streamlit run app.py
```
瀏覽器打開 `http://localhost:8501` 即可使用。

---

## ☁️ 部署指南 (Zeabur / Docker)

### 部署至 Zeabur
本專案已針對 [Zeabur](https://zeabur.com) 優化，可直接部署。

1. **建立服務**: 選擇 GitHub Repository 部署。
2. **環境變數**: 設定上述的 `GEMINI_API_KEY` 等變數。
3. **持久化儲存 (Volume)** (重要！):
   - 為了讓資料庫 (History/Schedules) 在重啟後不遺失，請掛載一個 Volume。
   - **Mount Path**: `/app/data`
   - **Variable**: 設定 `ZEABUR_VAR_DB_PATH=/app/data`
4. **完成**: Zeabur 會自動識別 `requirements.txt` 並開始構建。

---

## 🛠️ 技術架構 (Stack)
- **Frontend**: Streamlit
- **AI Core**: Google Gemini 1.5 Flash
- **Backend**: Python (APScheduler, SQLite)
- **Email**: SMTP (Standard Lib)

---

<div align="center">
    Built for Automation. Powered by AI.
</div>
